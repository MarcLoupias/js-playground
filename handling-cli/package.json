{
  "name": "@js-playground/handling-cli",
  "version": "1.0.0",
  "description": "Handling CLI with Node.js",
  "author": "marlou <pro@marc-loupias.fr>",
  "private": true,
  "license": "ISC",
  "engines": {
    "node": ">=6.0.0",
    "npm": ">=5.2.0"
  },
  "main": "index.js",
  "bin": {
    "cli": "index.js"
  },
  "scripts": {
    "commander-basic": "npx -p node@latest -- node commander-basic -p -c toto",
    "commander-version": "npx -p node@latest -- node commander-basic -v",
    "test": "npx -p node@latest -- mocha sync-test.js"
  },
  "dependencies": {
    "commander": "^2.19.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^5.2.0"
  }
}
